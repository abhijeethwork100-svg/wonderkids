{
  "version": 2,
  "name": "wonderkids",
  "buildCommand": "cd react-app && npm install && npx vite build",
  "outputDirectory": "react-app/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
